import 'package:flutter/material.dart';

class IceFactoryHomePage extends StatelessWidget {
  const IceFactoryHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Ice factory home page')));
  }
}
